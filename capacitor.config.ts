import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examenU3-Manitos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
